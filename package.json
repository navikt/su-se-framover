{
  "name": "su-se-framover",
  "version": "1.0.0",
  "description": "Frontend for saksbehandlingsstøtteverktøy for supplerende stønad",
  "scripts": {
    "test": "test",
    "start": "NODE_ENV=development parcel src/index.html --open",
    "build": "npm run clean:dist && parcel build src/index.html --out-dir dist/ --public-url /static --no-source-maps",
    "clean:dist": "rimraf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/navikt/su-se-framover.git"
  },
  "author": "supstonad",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/navikt/su-se-framover/issues"
  },
  "homepage": "https://github.com/navikt/su-se-framover#readme",
  "dependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0"
  }
}
