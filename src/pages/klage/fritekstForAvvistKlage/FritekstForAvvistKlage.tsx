import React from 'react';

import { Klage } from '~types/Klage';

const FritekstForAvvistKlage = (props: { klage: Klage }) => {
    console.log(props.klage);
    return <div>fyll inn fritekst</div>;
};

export default FritekstForAvvistKlage;
